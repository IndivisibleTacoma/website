@{
    string name = Model.ContentField.DisplayName;
    int faqId = Model.ContentItem.Id;
}

@if (HasText(name) && HasText(Model.Value)) {
    <button class="usa-button-unstyled" aria-expanded="false" aria-controls="collapsible-@faqId">@Model.Value</button>
    }
